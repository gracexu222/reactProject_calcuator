import React from "react";
import { personalInfo } from "./personInfo";

function UserList({personalInfo}){
    return(
        <div>
            personalInfo.map((User) =>(
                
            ))
        </div>
    );
}
